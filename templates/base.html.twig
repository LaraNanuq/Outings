<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8"/>
        <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <link rel="stylesheet" href="{{ asset('style/normalize.css') }}"/>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('style/style.css') }}"/>
        {% endblock %}
        {% block javascripts %}
            {# #}
        {% endblock %}
        <title>
            {% block title %}
                Sortir avec l'ENI
            {% endblock %}
        </title>
    </head>

    <body>
        <header class="mb-3 border rounded bg-light bg-gradient">
            {% include ('fragment/nav.html.twig') %}
        </header>

        <div>
            {% for label, messages in app.flashes %}
                <div class="flash flash-{{ label }} alert alert-{{ label }}">
                    {% for message in messages %}
                        <span class="flash-message">
                            {{ message }}
                            <br />
                        </span>
                    {% endfor %}
                </div>
            {% endfor %}
        </div>

        <main class="my-3 border border-secondary rounded bg-light bg-gradient p-3">
            {% block body %}
                {# #}
            {% endblock %}
        </main>

        <footer class="border rounded p-2 text-center">
            <div>
                &copy; {{ "now"|date("Y") }} - PoueTeam
            </div>
        </footer>
    </body>
</html>
