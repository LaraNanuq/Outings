<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8"/>
        <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <link rel="stylesheet" href="{{ asset('style/normalize.css') }}"/>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('style/style.css') }}"/>
        {% endblock %}
        {% block javascripts %}
            {# #}
        {% endblock %}
        <title>
            {% block title %}
                Sortir avec l'ENI
            {% endblock %}
        </title>
    </head>

    <body>
        <header>
            {# todo:  logo + lien #}
            <h1>Sortir.com</h1>
            {% include ('fragment/nav.html.twig') %}
        </header>

        <main>
            {% for label, messages in app.flashes %}
                <div class="flash flash-{{ label }}">
                    {% for message in messages %}
                        <p class="flash-message">
                            {{ message }}
                        </p>
                    {% endfor %}
                </div>
            {% endfor %}
            
            {% block body %}
                {# #}
            {% endblock %}
        </main>

        <footer>
            <div>
                &copy; {{ "now"|date("Y") }} - PoueTeam
            </div>
        </footer>
    </body>
</html>
