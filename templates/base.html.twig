<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8"/>
        <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <link rel="stylesheet" href="{{ asset('style/normalize.css') }}"/>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('style/style.css') }}"/>
        {% endblock %}
        {% block javascripts %}
            {# Scripts #}
        {% endblock %}
        <title>
            {% block title %}
                Sortir à l'ENI
            {% endblock %}
        </title>
    </head>

    <body>
        <header>
            {% include ('fragment/header.html.twig') %}
        </header>

        <div>
            {% for label, messages in app.flashes %}
                <div class="flash flash-{{ label }} alert alert-{{ label }}">
                    {% for message in messages %}
                        <span class="flash-message">
                            {{ message }}
                            <br />
                        </span>
                    {% endfor %}
                </div>
            {% endfor %}
        </div>

        <main class="my-3 border border-secondary rounded bg-light bg-gradient p-3">
            {% block body %}
                {# Content #}
            {% endblock %}
        </main>

        <footer>
            {% include ('fragment/footer.html.twig') %}
        </footer>
    </body>
</html>
