<nav class="navbar navbar-expand-sm navbar-light">
    <div class="container-fluid">
        <a href="{{ path('main_home') }}" class="navbar-brand">Sortir à l'ENI</a>
        
        {% if (app.user) %}
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="{{ path('main_home') }}" class="nav-link">Accueil</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('user_edit') }}" class="nav-link">Mon profil</a>
                </li>
                {% if (is_granted("ROLE_ADMIN")) %}
                    <li class="nav-item">
                        <a href="{{ path('dashboard_city_list') }}" class="nav-link">Villes</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('dashboard_campus_list') }}" class="nav-link">Campus</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a href="{{ path('auth_logout') }}" class="nav-link text-danger">Déconnexion</a>
                </li>
            </ul>
        {% endif %}
    </div>
</nav>
